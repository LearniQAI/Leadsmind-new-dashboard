// The one hostname customers point their custom-domain CNAME at. Must be a domain LeadsMind
// owns and has attached to the Vercel project — leadsmind.io is the owned domain.
export const CUSTOM_DOMAIN_CNAME_TARGET = 'domains.leadsmind.io';
