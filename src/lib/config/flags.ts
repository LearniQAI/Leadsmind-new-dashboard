/**
 * Master switch to temporarily pause all plan/upgrade restrictions across the app for testing.
 * Set to `false` to pause all limits/warnings/prompts and allow unlimited access.
 * Set to `true` to enforce standard subscription tiers and credit guards.
 */
export const ENFORCE_PLAN_LIMITS = false;
